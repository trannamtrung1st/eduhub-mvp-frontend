import { StyleSanitizedPipe } from './style-sanitized.pipe';

describe('StyleSanitizedPipe', () => {
  it('create an instance', () => {
    const pipe = new StyleSanitizedPipe();
    expect(pipe).toBeTruthy();
  });
});

export const IDENTITY_STATES = {
    identity: {
        name: 'identity',
        currentUser: {
            name: 'currentUser',
            states: {}
        }
    }
};

export const IDENTITY_LOCAL_STORAGE = {
    currentUser: {
        key: 'currentUser'
    }
}
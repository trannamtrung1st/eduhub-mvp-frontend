import { LoginHandler } from "./log-in/log-in.handler";
import { LogoutHandler } from "./log-out/log-out.handler";

const IdentityCommandHandlers = null;

export {
    LoginHandler as Login,
    LogoutHandler as Logout,
    IdentityCommandHandlers
}